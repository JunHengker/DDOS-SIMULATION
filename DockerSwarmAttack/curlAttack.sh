#!/bin/bash
while true
do
  # Please use 'man curl' to see what -vk is for, also -X command can be useful too.
  # Replace <ip> with the actual IP or URL of the victim server
  # curl -vk https://<ip>
  # Uncomment sleep if needed to avoid overwhelming the server
  # sleep 5
  # testing with echos
    echo "curl -vk https://<ip>"
done
exit 0